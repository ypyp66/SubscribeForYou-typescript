export interface create_intialState {
  title: string;
  price: number | string;
  day: number | string;
}

const Create_InitialState: create_intialState = {
  title: "",
  price: "",
  day: "",
};

export default Create_InitialState;
