import Main from "../components/Main";
import HomeContainer from "../containers/MainContainer";

function Home({ user, subscribeDatas }) {
  return (
    <div>
      <HomeContainer />
    </div>
  );
}

export default Home;
